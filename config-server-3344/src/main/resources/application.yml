server:
  port: 3344

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/ttxxi/SpringCloudConfigDemo/
          search-paths: respo
          username:
          password:
      label: master
